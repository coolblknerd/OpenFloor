<h1>This is OpenFloor</h1> 

<% @posts.each do |post| %>

  <h2><%= link_to post.title, post_path(post) %></h2>
  
  <p>Published at <%= time_ago_in_words(post.created_at)%></p>
    
<% end %><br>

<%= link_to "New Post", new_post_path %>